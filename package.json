{
  "name": "automata",
  "version": "0.1.0",
  "description": "Systematic multi-platform application engine",
  "main": "dist/main.js",
  "type": "module",
  "scripts": {
    "build": "tsc",
    "fetch-targets": "python scripts/build_targets.py",
    "start": "tsc && node dist/main.js",
    "deploy": "npm run fetch-targets && npm run start"
  },
  "dependencies": {
    "dotenv": "^16.4.5",
    "playwright": "^1.49.0"
  },
  "devDependencies": {
    "@types/node": "^20.11.0",
    "typescript": "^5.3.3"
  }
}